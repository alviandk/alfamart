{% extends "main.html" %}
{% block main %}
    {% load staticfiles %}
    <!-- Page Heading -->

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Detail Stok <!--<small>Statistics Overview</small>-->
            </h1>
            <ol class="breadcrumb">
                <li class="active">
                    <i class="fa fa-home"></i> <a href="{% url 'home' %}">Home</a>
                </li>
                <li>
                    <i class="fa fa-user"></i> Menu
                </li>
                <li>
                    <i class="fa fa-user"></i> Detail Stok
                </li>
            </ol>
        </div>
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-6">





    no_telepon
    email
    logo
            <h1>{{ object.kode_stok }}</h1>

            <p>{{ object.nama_stok }}</p>

            <p>Reporter: {{ object.kota }}</p>

            <p>Published: {{ object.kode_pos }}</p>

        </div>
    </div>
{% endblock %}
